{
  "name": "tutorial-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run setup && nodemon",
    "setup": "npm run build:frontend && npm run migrate:dev",
    "build:frontend": "cd ../wam && npm run build",
    "migrate:dev": "npm run db:up && prisma migrate dev --name init",
    "db:up": "docker-compose -f docker-compose.dev.yml up -d",
    "db:down": "docker-compose -f docker-compose.dev.yml down",
    "prisma:generate": "prisma generate",
    "prisma:studio": "npm run db:up && prisma studio",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.21.1",
    "@types/express": "^4.17.21",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "typescript": "^5.5.2"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "nodemon": "^3.1.4",
    "prisma": "^5.21.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0"
  }
}
